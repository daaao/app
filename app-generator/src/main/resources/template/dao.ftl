package ${daoPackageName};

import io.zhijian.base.dao.IBaseDao;
import ${entityPackageName}.${modelName};

/**
 * ${modelName} DAO接口
 * @create ${.now}
 */
public interface I${modelName}Dao extends IBaseDao<${modelName}, ${pkType}> {

}
